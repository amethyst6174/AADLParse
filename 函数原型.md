org.eclipse.emf子包 xtext的EMF模型操作架构，任何使用xtext解析的项目通常都存在这个package族

org.osate.etext.aadl2子包 定义aadl类型

|方法|返回值|说明|JPype示例|
|---|---|---|---|
| ResourceSetImpl()| ResourceSet对象| 初始化|rs = ResourceSetImpl()|
| createFileURI()| URI对象| 初始化目标路径||
| getResource(uri, bool)| Resource对象| 加载AADL文件|resource = rs.getResource(aadl_uri, True)|
| getContents()| EList<EObject>| 模型根列表，指package|model = resource.getContents().get(0)|
| getErrors()| EList<Diagnostic>| 文件解析错误列表|errors = resource.getErrors();|

通用组合
| 方法|返回值|说明|
|---|---|---|
| getCategory|xxx|返回当前组件类别（出自枚举类型例如BUS(1, bus, bus)），但直接输出会默认出发toString()|
| getName|string|返回当前组件名称|
| getOwnedSubcomponents|EList<Subcomponent>| 获取所有子组件，即当前实现中直接包含的子组件|
| getClassifierFeatures| EList<Feature>| 获取分类器特征（如端口、子组件等|
| getOwnedMembers| EList<Member> | 获取当前实现的所有成员，包括特征和子组件等 |
| isNoSubcomponents| boolean   | 判断当前实现是否没有子组件 |
| setNoSubcomponents| void  | 设置当前实现是否没有子组件|
| isNoCalls| boolean   | 判断当前实现是否没有调用 |
| setNoCalls| void  | 设置当前实现是否没有调用|
| getOwnedEventSources| EList<EventSource>| 获取所有事件源（如事件端口）|
| createOwnedEventSource| EventSource   | 创建一个新的事件源并返回|
| getOwnedEventDataSources  | EList<EventDataSource>| 获取所有事件数据源|
| createOwnedEventDataSource| EventDataSource   | 创建并返回一个新的事件数据源|
| isNoConnections| boolean   | 判断当前实现是否没有连接 |
| setNoConnections| void  | 设置当前实现是否没有连接|
| getOwnedFlowImplementations| EList<FlowImplementation> | 获取所有流实现|
| createOwnedFlowImplementation | FlowImplementation| 创建并返回一个新的流实现|
| getOwnedConnections| EList<Connection> | 获取当前实现的所有连接  |
| getOwnedProcessorFeatures | EList<ProcessorFeature>   | 获取所有处理器特征|
| getOwnedInternalFeatures  | EList<InternalFeature>| 获取所有内部特征|
| getType| ComponentType | 获取当前实现的组件类型  |
| basicGetType| ComponentType | 获取组件的基本类型 |
| setType| void  | 设置当前实现的组件类型  |
| getOwnedExtension| EList<Extension>  | 获取当前实现的所有扩展  |
| setOwnedExtension| void  | 设置当前实现的扩展 |
| createOwnedExtension| Extension | 创建并返回一个新的扩展 |
| getOwnedRealization| EList<Realization>| 获取所有实现  |
| basicSetOwnedRealization| void  | 设置当前实现的基本实现 |
| setOwnedRealization| void  | 设置当前实现的实现 |
| createOwnedRealization| Realization   | 创建并返回一个新的实现 |
| getOwnedAbstractSubcomponents| EList<AbstractSubcomponent>   | 获取所有抽象子组件 |
| createOwnedAbstractSubcomponent| AbstractSubcomponent  | 创建并返回一个新的抽象子组件|
| getOwnedAccessConnections| EList<AccessConnection>   | 获取所有访问连接  |
| createOwnedAccessConnection| AccessConnection  | 创建并返回一个新的访问连接 |
| getOwnedParameterConnections| EList<ParameterConnection>| 获取所有参数连接  |
| createOwnedParameterConnection| ParameterConnection   | 创建并返回一个新的参数连接 |
| getOwnedPortConnections| EList<PortConnection> | 获取所有端口连接  |
| createOwnedPortConnection| PortConnection| 创建并返回一个新的端口连接 |
| getOwnedFeatureConnections| EList<FeatureConnection>  | 获取所有特征连接  |
| createOwnedFeatureConnection| FeatureConnection | 创建并返回一个新的特征连接|
| getOwnedFeatureGroupConnections| EList<FeatureGroupConnection> | 获取所有特征组连接|
| createOwnedFeatureGroupConnection| FeatureGroupConnection| 创建并返回一个新的特征组连接  |
| getOwnedEndToEndFlows| EList<EndToEndFlow>   | 获取所有端到端流  |
| createOwnedEndToEndFlow| EndToEndFlow  | 创建并返回一个新的端到端流|
| eInverseRemove| void  | 从反向关联中移除指定元素|
| toString| String| 返回当前对象的字符串表示|
| isSetGenerals| boolean   | 判断是否已设置父类 |
| isSetOwnedSubcomponents| boolean   | 判断是否已设置子组件 |
| isSetClassifierFeatures| boolean   | 判断是否已设置分类器特征 |
| isSetOwnedMembers| boolean   | 判断是否已设置成员 |
| isSetOwnedConnections| boolean   | 判断是否已设置连接 |
| addConnection| void  | 向当前组件实现中添加连接|
| getTypeName| String| 获取当前组件类型的名称 |
| getImplementationName| String| 获取当前组件实现的名称 |
| getAllConnections| EList<Connection> | 获取所有连接（包括当前实现和继承的连接）|
| getIngoingConnections| EList<Connection> | 获取所有传入的连接 |
| getAllSubcomponents| EList<Subcomponent>   | 获取所有子组件（包括当前实现和继承的子组件）|
| getAllFeatures| EList<Feature>| 获取所有特征（包括当前实现和继承的特征）   |
| getAllModes| EList<Mode>   | 获取所有模式  |
| getAllModeTransitions| EList<ModeTransition> | 获取所有模式转换  |
| getAllPrototypes| EList<Prototype>  | 获取所有原型  |
| getAllEndToEndFlows| EList<EndToEndFlow>   | 获取所有端到端流  |
| getAllFlowImplementations| EList<FlowImplementation> | 获取所有流实现|
| isDescendentOf| boolean| 判断当前实现是否为指定父类的子类   |
| getPropertyValueInternal| Object| 获取内部属性值|
| lookupPrototypeBinding| PrototypeBinding  | 查找指定的原型绑定|
| lookupPrototypeBindingHelper  | PrototypeBinding  | 辅助方法，用于查找原型绑定 |
| findNamedElement| NamedElement  | 查找指定名称的元素|
| getAllInternalFeatures| EList<InternalFeature>| 获取所有内部特征  |
| getAllEventSources| EList<EventSource>| 获取所有事件源|
| getAllEventDataSources| EList<EventDataSource>| 获取所有事件数据源|
| getAllProcessorFeatures| EList<ProcessorFeature>   | 获取所有处理器特征|

子类方法
https://github.com/osate/osate2/tree/master/core/org.osate.aadl2/src/org/osate/aadl2/impl